[pytest]
markers =
    unit: fast, isolated unit tests (no network, no filesystem)
    integration: tests that hit the FastAPI app or services together
    e2e: end-to-end browser tests
filterwarnings =
    ignore::DeprecationWarning
